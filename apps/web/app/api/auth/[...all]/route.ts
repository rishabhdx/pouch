import { authHandler } from "@pouch/auth/server";

export const { GET, POST } = authHandler;

// import { handler } from "@pouch/backend/better-auth/server";

// export const { GET, POST } = handler;
